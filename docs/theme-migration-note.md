# Theme implementation note

The Sellpy-inspired visual changes are isolated to `app/sellpy-theme.css` and the global font/import changes in `app/layout.tsx`. No business logic, database schema, auth, commission, pickup, checkout or RLS behavior is changed by the theme itself.
